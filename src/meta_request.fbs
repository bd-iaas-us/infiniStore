//RDMA read/allocate request
table RemoteMetaRequest {
  keys: [string];
  block_size: int;
  rkey: uint;                  // rkey
  remote_addrs: [ulong];       // GPU addresses
  op: byte;
  txn_id: string;
}
root_type RemoteMetaRequest;
